import Link from 'next/link';
import Layout from '../../components/layout';

export default function FirstPost() {
   return (
      <Layout>
      </Layout>
   )
}

// pages will associate with route, you can access this template with localhost:3000/posts/first-post
// simply make pages directory and the path to the file becomes the URL path